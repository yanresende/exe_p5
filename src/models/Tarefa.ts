class Tarefa {
  nome: string
  email: string
  id: number
  telefone: number

  constructor(nome: string, email: string, id: number, telefone: number) {
    this.nome = nome
    this.email = email
    this.id = id
    this.telefone = telefone
  }
}

export default Tarefa
