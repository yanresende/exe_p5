class Tarefa {
  nome: string
  email: string
  id: number
  telefone: string

  constructor(nome: string, email: string, id: number, telefone: string) {
    this.nome = nome
    this.email = email
    this.id = id
    this.telefone = telefone
  }
}

export default Tarefa
